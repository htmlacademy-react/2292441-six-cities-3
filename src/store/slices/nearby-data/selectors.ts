import { State } from '../../../types/state';

export const SelectNearbyOffers = (state: State) => state.NEARBY.nearbyOffers;
export const SelectNearbyRequestStatus = (state: State) => state.NEARBY.requestStatus;
